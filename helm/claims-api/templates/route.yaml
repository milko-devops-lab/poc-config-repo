apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "claims-api.name" . }}
  labels:
    {{- include "claims-api.labels" . | nindent 4 }}
spec:
  to:
    kind: Service
    name: {{ include "claims-api.name" . }}
  port:
    targetPort: http